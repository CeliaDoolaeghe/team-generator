const pickRandomly = () => (Math.random() > 0.5) ? 1 : 0;

export const generateTeams = (players) => {
  const team1 = [];
  const team2 = [];

  // Put players in teams

  return { team1, team2 };
};
